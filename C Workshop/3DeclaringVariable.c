#include <stdio.h> // this is a library, which enables printf

// this is a comment
/* 
    this is a multi line comment
*/

int main (void){

    int x; // initialises the variable
    int y;
    int z;
    double w;
    x = 22; // assignment of the variable
    y = 33;
    z = x + y;
    w = 9;
    printf("Hello Person the numbers are: \n x is %d \n y is %d \n",x,y );
    printf("Value of w is  %lf\n ", w);

    
    /* print f means print format*/
    return 0;
}
